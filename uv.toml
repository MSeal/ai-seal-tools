# This file is intentionally minimal.
# The work-global ~/.config/uv/uv.toml routes uv through Confluent's CodeArtifact,
# which doesn't apply here. Run uv with UV_NO_CONFIG=1 (see .envrc / direnv).
[[index]]
url = "https://pypi.org/simple/"
default = true
